# General Settings
SEED: 42
USE_GPU: True
DOWNLOAD_DATASET: True  # Checks if the user wants to download the dataset
USE_CACHED_SCALOGRAMS: True # Checks if the user wants to use the cached scalograms 
USE_GPU: True

# Training Parameters
NUM_EPOCH: 5
BATCH_SIZE: 32
LEARNING_RATE: 0.001
DATA_SUBSET_SIZE: 128  # Set to None to use the full dataset
#Be careful with the DATA_SUBSET_SIZE parameter, you have to change USE_CACHED_SCALOGRAMS to False the first time


